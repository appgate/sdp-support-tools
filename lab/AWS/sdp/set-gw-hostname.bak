---
- hosts: gateways
  gather_facts: no
  vars_files:
    - ../windowsDC/tf_ansible_vars_file.yml
    - tf_ansible_vars_file.yml

  tasks:

    - name: set gateway hostname 
      hostname:
        name: "gateway01.{{ tf_dc_domain }}"
